# rspec spec/lib/tweet_spec.rb

require "spec_helper"
require "tweet"




=begin
describe Zombie do
	# tests go here
	it "is named Ash and it has no brains" do
		zombie = Zombie.new
		expect(zombie.name).to eq('Ash')
		expect(zombie.brains).to be < 1
	end

	it "is hungry" do
		zombie = Zombie.new
		expect(zombie.hungry?).to be_hungry
	end
end
=end